<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="5dp"
    android:layout_margin="5dp"
    android:descendantFocusability="blocksDescendants"
    android:focusable="false"
    android:clickable="false">

    <ImageView
        android:id="@+id/imagemViewPedido"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginTop="5dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        />

    <TextView
    android:id="@+id/textNamePedido"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_toRightOf="@+id/imagemViewPedido"
    android:text="Name"
    android:textStyle="bold"
    android:textSize="20dp"
    android:textAlignment="center"
    />

    <TextView
        android:id="@+id/textPrecoPedido"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:text="R$"
        android:textStyle="bold"
        android:textSize="20dp"
        android:textAlignment="center"
        android:layout_alignBottom="@+id/imagemViewPedido"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="false"
        android:layout_toRightOf="@+id/imagemViewPedido"
        android:layout_toEndOf="@+id/imagemViewPedido" />

    <TextView
        android:id="@+id/textQuantidadePedido"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:text="Unidades"
        android:textStyle="bold"
        android:textSize="20dp"
        android:textAlignment="center"
        android:layout_alignBottom="@+id/imagemViewPedido"
        android:layout_alignRight="@+id/textPrecoPedido" />

</RelativeLayout>